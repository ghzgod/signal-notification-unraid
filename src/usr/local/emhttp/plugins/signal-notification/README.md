**Signal Notification Agent**

Send Unraid system notifications to Signal groups via signal-cli's JSON-RPC API.

Requires a running signal-cli instance. Configure at Settings > Notifications > Signal.
